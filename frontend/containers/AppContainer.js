import PropTypes from 'prop-types';
import React from 'react';
import { connect } from 'react-redux';
import Public from './Students';

const AppContainer = () => {
    return (
        <div>
            <Public />
        </div>
    );
};

export default AppContainer;
